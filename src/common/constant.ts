import { ContainerType } from '@/interface';

export const tools = [
    { name: 'Cookies', key: ContainerType.cookies },
    { name: 'Hosts', key: ContainerType.hosts },
    { name: 'Storages', key: ContainerType.storages }
];
